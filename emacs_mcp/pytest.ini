[pytest]
# Pytest configuration for emacs_mcp

markers =
    integration: marks tests as integration tests (require running Emacs server)

# By default, skip integration tests
# Run with: pytest -v -m integration
# Or run all: pytest -v
addopts = -v
